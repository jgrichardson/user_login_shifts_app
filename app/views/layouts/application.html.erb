<!DOCTYPE html>
<html>
  <head>
    <title>User Login Shifts Application</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <div id="navbarBasicExample" class="navbar-menu">
      <div class="navbar-start">
        <a class="navbar-item" href=<%= root_path%>>HOME</a>
        <a class="navbar-item" href=<%= users_path%>>ALL USERS</a>
        <a class="navbar-item" href=<%= shifts_path%>>ALL SHIFTS</a>
        <div class="navbar-item has-dropdown is-hoverable">
          <a class="navbar-link">MORE</a>
          <div class="navbar-dropdown">
            <a class="navbar-item" href="https://github.com/jgrichardson/user_login_shifts_app" target="_blank">SOURCE CODE</a>
          </div>
        </div>
      </div>
      <div class="navbar-end">
        <div class="navbar-item">
          <div class="buttons">
          <% if logged_in? %>
            <a class="button is-primary" href=<%= user_shifts_path(user_id: current_user.id) %>>
              <strong>My Shifts</strong>
            </a>
            <a class="button is-primary" href=<%= session_path %> data-method="delete">
              <strong>Logout</strong>
            </a>
          <% else %>
            <a class="button is-primary" href=<%= new_session_path %>>
              <strong>Login</strong>
            </a>
          <% end %>
            
          </div>
        </div>
      </div>
    </div>
  </head>
  <body>
    <section class="section">
      <div class="content">
        <div class="container">
          <div class="columns">
            <div class="column">
              <% flash.each do |type, msg| %>
                <div class="notification is-primary">
                  <%= msg %>
                </div>
              <% end %>
              <%= yield %>
            </div>
          </div>
        </div>
      </div>
    </section>
  </body>
</html>